# Vendored Three.js r160

This folder already contains:

- `three.module.js`
- `addons/controls/OrbitControls.js`
- `addons/controls/PointerLockControls.js`

`index.html` import map points here so the game runs fully offline once the zip is unpacked.
